service: opensearch-training
frameworkVersion: "3"

plugins:
  - serverless-esbuild

provider:
  name: aws
  runtime: nodejs18.x
  region: ap-northeast-1
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - es:ES*
          Resource: 
            - arn:aws:es:ap-northeast-1:744743450114:domain/opensearch-training
  vpc:
    subnetIds:
      - subnet-090bfef9969da7c18
      - subnet-0f35817cff4732065
    securityGroupIds:
      - sg-0306324f35bfea072

functions:
  searchCalls:
    handler: handler.searchCalls
    events:
      - http:
          path: /api/v1/calls
          method: GET
  addCallDocument:
    handler: handler.addCallDocument
    events:
      - http:
          path: /api/v1/calls
          method: POST